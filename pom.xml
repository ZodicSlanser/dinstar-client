<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.zodicslanser</groupId>
    <artifactId>dinstar-client</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Dinstar UC2000-VG HTTP API client</name>
    <description>
        Thin, typed Java client for the Dinstar UC2000-VG GSM gateway HTTP API (v202011).
        Firmware-agnostic. Not a Spring application — a plain constructable library.
    </description>
    <url>https://github.com/ZodicSlanser/dinstar-client</url>

    <licenses>
        <!-- Change this if you prefer a different license (also update the LICENSE file). -->
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>ZodicSlanser</id>
            <name>ZodicSlanser</name>
            <url>https://github.com/ZodicSlanser</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/ZodicSlanser/dinstar-client.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/ZodicSlanser/dinstar-client.git</developerConnection>
        <url>https://github.com/ZodicSlanser/dinstar-client</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <httpclient5.version>5.4.1</httpclient5.version>
        <jackson.version>2.18.2</jackson.version>
        <spring-boot.version>3.4.1</spring-boot.version>
        <junit.version>5.11.4</junit.version>
    </properties>

    <dependencies>
        <!-- Core: HTTP transport (built-in RFC-7616 Digest) + JSON. These are the only
             dependencies that reach a consumer of this library. -->
        <dependency>
            <groupId>org.apache.httpcomponents.client5</groupId>
            <artifactId>httpclient5</artifactId>
            <version>${httpclient5.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!-- Optional Spring Boot auto-configuration for the single-gateway convenience case.
             `optional` => it does NOT leak onto a consumer's classpath. The auto-config classes
             only load when the consumer already has Spring Boot. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <version>${spring-boot.version}</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <version>${spring-boot.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Test only -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
            </plugin>
            <!-- Publish sources + javadoc alongside the JAR. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals><goal>jar-no-fork</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.11.2</version>
                <configuration>
                    <!-- Check javadoc syntax/references but don't fail the build on a
                         missing tag; every public type is documented in prose regardless. -->
                    <doclint>all,-missing</doclint>
                    <quiet>true</quiet>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals><goal>jar</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!--
      Release to Maven Central: `mvn -Prelease deploy`.
      Kept in a profile so ordinary `mvn test`/`package` need no GPG key or Central credentials.
      Prerequisites (see README "Publishing"): a verified io.github.zodicslanser namespace,
      a published GPG key, a `central` server entry in ~/.m2/settings.xml, and gpg on PATH.
    -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals><goal>sign</goal></goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.7.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <!-- autoPublish=false lets you review the deployment in the Central
                                 Portal UI and click "Publish" manually; set true to auto-release. -->
                            <autoPublish>false</autoPublish>
                            <waitUntil>validated</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
